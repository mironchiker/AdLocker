name: Build and Release AdLocker

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.x'
          channel: 'stable'

      - name: Generate Android project structure
        run: |
          if [ ! -d "android" ]; then
            flutter create . --platforms=android --project-name=adlocker --org=com.adlocker.app
          fi

      - name: Configure Android Permissions and VPN Service
        run: |
          # Разрешения в манифест
          sed -i '/<manifest/a \    <uses-permission android:name="android.permission.INTERNET"/>\n    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>\n    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>' android/app/src/main/AndroidManifest.xml || true
          
          # Регистрация VpnService в AndroidManifest.xml
          sed -i '/<\/activity>/a \        <service android:name=".AdLockerVpnService" android:permission="android.permission.BIND_VPN_SERVICE" android:exported="false">\n            <intent-filter>\n                <action android:name="android.net.VpnService"/>\n            </intent-filter>\n        </service>' android/app/src/main/AndroidManifest.xml || true

      - name: Inject Native Android VPN Bridge
        run: |
          PACKAGE_DIR="android/app/src/main/kotlin/com/adlocker/app"
          if [ ! -d "$PACKAGE_DIR" ]; then
            PACKAGE_DIR=$(find android/app/src/main -type d -name "adlocker" | head -n 1)
          fi
          
          cat << 'EOF' > "$PACKAGE_DIR/AdLockerVpnService.kt"
          package com.adlocker.app

          import android.content.Intent
          import android.net.VpnService
          import android.os.ParcelFileDescriptor

          class AdLockerVpnService : VpnService() {
              private var vpnInterface: ParcelFileDescriptor? = null

              override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
                  if (intent?.action == "STOP") {
                      stopVpn()
                      return START_NOT_STICKY
                  }
                  startVpn()
                  return START_STICKY
              }

              private fun startVpn() {
                  if (vpnInterface != null) return
                  try {
                      val builder = Builder()
                      builder.setSession("AdLocker Shield")
                      builder.addAddress("10.0.0.2", 32)
                      // Заворачиваем системный DNS на публичный DNS AdGuard / Cloudflare с фильтром
                      builder.addDnsServer("94.140.14.14") 
                      builder.addDnsServer("94.140.15.15")
                      builder.setBlocking(false)
                      vpnInterface = builder.establish()
                  } catch (e: Exception) {
                      e.printStackTrace()
                  }
              }

              private fun stopVpn() {
                  try {
                      vpnInterface?.close()
                      vpnInterface = null
                      stopSelf()
                  } catch (e: Exception) {
                      e.printStackTrace()
                  }
              }

              override fun onDestroy() {
                  stopVpn()
                  super.onDestroy()
              }
          }
          EOF

          cat << 'EOF' > "$PACKAGE_DIR/MainActivity.kt"
          package com.adlocker.app

          import android.app.Activity
          import android.content.Intent
          import android.net.VpnService
          import io.flutter.embedding.android.FlutterActivity
          import io.flutter.embedding.engine.FlutterEngine
          import io.flutter.plugin.common.MethodChannel

          class MainActivity: FlutterActivity() {
              private val CHANNEL = "com.adlocker.app/vpn"
              private val VPN_REQUEST_CODE = 1001
              private var pendingResult: MethodChannel.Result? = null

              override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
                  super.configureFlutterEngine(flutterEngine)
                  MethodChannel(flutterEngine.dartExecutor.binaryMessenger, CHANNEL).setMethodCallHandler { call, result ->
                      when (call.method) {
                          "startVpn" -> {
                              val intent = VpnService.prepare(this)
                              if (intent != null) {
                                  pendingResult = result
                                  startActivityForResult(intent, VPN_REQUEST_CODE)
                              } else {
                                  startServiceDirectly()
                                  result.success(true)
                              }
                          }
                          "stopVpn" -> {
                              val intent = Intent(this, AdLockerVpnService::class.java).apply { action = "STOP" }
                              startService(intent)
                              result.success(true)
                          }
                          else -> result.notImplemented()
                      }
                  }
              }

              private fun startServiceDirectly() {
                  val intent = Intent(this, AdLockerVpnService::class.java)
                  startService(intent)
              }

              override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
                  super.onActivityResult(requestCode, resultCode, data)
                  if (requestCode == VPN_REQUEST_CODE) {
                      if (resultCode == Activity.RESULT_OK) {
                          startServiceDirectly()
                          pendingResult?.success(true)
                      } else {
                          pendingResult?.success(false)
                      }
                      pendingResult = null
                  }
              }
          }
          EOF

      - name: Get dependencies
        run: flutter pub get

      - name: Build Single Release APK
        run: |
          flutter build apk --release --target-platform=android-arm,android-arm64 --no-shrink

      - name: Sign APK (V1 + V2 + V3)
        run: |
          keytool -genkeypair -v -keystore release.keystore -alias adlocker \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass adlocker123 -keypass adlocker123 \
            -dname "CN=AdLocker, OU=App, O=AdLocker, L=City, S=State, C=RU"

          APKSIGNER=$(find $ANDROID_SDK_ROOT/build-tools -name apksigner | sort -V | tail -n 1)
          
          $APKSIGNER sign --ks release.keystore \
            --ks-key-alias adlocker \
            --ks-pass pass:adlocker123 \
            --key-pass pass:adlocker123 \
            --out build/app/outputs/flutter-apk/app-release-signed.apk \
            build/app/outputs/flutter-apk/app-release.apk

      - name: Prepare APK for Release
        run: |
          mkdir -p release_apks
          cp build/app/outputs/flutter-apk/app-release-signed.apk release_apks/AdLocker.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.0-${{ github.run_number }}
          name: AdLocker v1.0.0 (Build ${{ github.run_number }})
          body: |
            ## AdLocker — Shield & Real Ad Blocker
            * Полноценный нативный Android VpnService.
            * Хранение базы 70 000+ правил в локальном хранилище.
          files: release_apks/AdLocker.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

